server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  redis:
    host: ${DB_HOST}
    port: ${REDIS_PORT}

app:
  cache-timeout: 300